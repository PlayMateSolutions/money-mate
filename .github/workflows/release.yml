name: Build Android Release APK

on:
  workflow_dispatch:

jobs:
  build-android-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Ionic CLI
        run: npm install -g @ionic/cli

      - name: Install dependencies
        run: |
          npm ci --legacy-peer-deps
          npm i -D -E @angular/cli --legacy-peer-deps

      - name: Build web assets
        run: ionic build --configuration production

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Grant permission
        run: chmod +x android/gradlew

      - name: Decode keystore
        env:
          ENCODED_KEYSTORE: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          echo $ENCODED_KEYSTORE | base64 -d > android/app/GymMate

      - name: Debug keystore and credentials
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          echo "Checking keystore file..."
          ls -la android/app/GymMate || echo "Keystore file not found"
          file android/app/GymMate || echo "Cannot determine file type"
          
          echo "Testing keystore accessibility..."
          keytool -list -keystore android/app/GymMate -storepass "$KEYSTORE_PASSWORD" || echo "Failed to access keystore"
          
          echo "Checking if alias exists..."
          keytool -list -keystore android/app/GymMate -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS" || echo "Alias not found or wrong password"

      - name: Build Release APK
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          cd android
          ./gradlew assembleRelease

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: money-mate-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk

      - name: Clean up keystore
        if: always()
        run: rm -f android/app/GymMate